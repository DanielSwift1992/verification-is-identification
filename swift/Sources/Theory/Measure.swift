// ═══════════════════════════════════════════════════════════════
// The Measured Geometry of ARC-AGI-1 (Measurement) — paper E1.
//
// The theory derives a chain, but its load-bearing CONDITIONAL claims —
// modulus (G10), compressibility (G12), navigability at small radius (N11),
// strategy clustering (O15), alignment (α, GA) — are NOT derivable from
// I1–I3. They are measurable properties of a domain. This document is the
// instrument: ARC-AGI-1 (400 train / 400 eval), the correct domain because
// it was designed to defeat r=0 (memorization). It asks the one question
// that separates this framework from a cache: does the rule space cluster?
//
// This adds no new structure — it MEASURES the conditional protocols already
// in the lattice. So it pins nothing and opens no axis; it bundles the
// measurable conditionals and names the proven/measured split. [Measure E1]
// ═══════════════════════════════════════════════════════════════

/// The conditional faces the theory cannot derive from I1–I3 are what an
/// instrument must MEASURE. Searchability (clustered ∧ aligned,
/// ``SearchableIffClusteredAndAligned``) and the ``FourthFace``, skill
/// compressibility (O18), are bundled as exactly those properties. The fork is
/// fixed in advance: the rule space CLUSTERS (N(diam/4) ≤ K/4, δ ≤ diam/8 —
/// searchable) or is FLAT (a theorem-grade negative: "nothing to learn, only
/// memorize"). Both branches are results; the boundary is a number you compute,
/// not an opinion. [Measure E1]
///
/// > Note: This is the line between the proven core (Theorems 1–5, unconditional —
/// > ``PassIsIdentification`` and its ancestry) and the measured core — the
/// > conditional faces a domain either has or lacks. The instrument is empirical,
/// > so it lives in `python/`, not in these types.
public protocol MeasuredGeometry: SearchableIffClusteredAndAligned, FourthFace {}

// ───────────────────────────────────────────────────────────────
// The instrument (P1–P10) — recorded, not type-level.
//
// E1 separates the PROVEN core (I1–I3, Theorems 1–5 — derived, unconditional:
// PassIsIdentification and its ancestry) from the MEASURED core (the conditional
// protocols above — domain properties). Phases: 0 encode → 1 bootstrap the
// crystal (the certified library, unique by SAT5/6) → 2 measure W-matrix /
// N(r) / δ / ρ_nav (P2–P5) → 3 inference on eval, W-ordered vs random (P6) →
// 4 coupon-collector + hole-curriculum (P8/P9) → 5 trace-replay strategies
// (P10). The ledger has four honest terminals — PASS / |S|>1 / EMPTY / BUDGET —
// so no silent failure exists by construction. Every number tests a named
// statement; both outcomes of every fork are results. The measurement is
// empirical, so it lives in `python/`, not in these types.
// ───────────────────────────────────────────────────────────────
