# Sources

The papers this lattice encodes — readable here, in full.

## Overview

Each protocol in ``Theory`` encodes a claim first made and proved in one of the papers below. The relationship runs one way: the papers are the *source* — prose derivations carrying the full proofs and the mathematics — and the lattice is their type-level *encoding*.

One paper sits apart. *Existence Is Finite* (`EiF`) is the **ground**, below the protocol floor: it argues the seed (``Pair``, ``Null``) and the medium Σ into place, so no protocol *routes* to it — it is what the rest are built in. Its claims surface as provenance on the seed and the kernel, never as a route of their own.

They are arranged differently, on purpose. The papers follow the order they were written. This documentation follows the structure *measured* from the lattice itself — a small basis of axes spanned by a web of theorems (see the ``Theory`` overview). The papers are one walk through that structure; the documentation is the structure.

So a tag like `[Nav N5]` at the end of a symbol's discussion is **provenance, not navigation** — it points to the section of the paper that proves the claim, where the full notation lives. To understand a symbol, its own page is enough; to read the derivation and the math, open the paper.

> Note: The lattice is an encoding, not a transcript. Some corollaries the papers state in prose are recorded here as comments rather than protocols, and the paper order is deliberately not the dependency order. The documentation never asks you to open a paper to understand what a symbol *is* — only to see how it was *proved*.

## The tag legend

The tag in each symbol's discussion decodes here. Every paper is readable in full at its link.

| Tag | Paper |
| --- | --- |
| `EiF` | [0 · Existence Is Finite](doc:0_existence_is_finite) |
| `V=I` | [1 · Verification Is Identification](doc:1_verification_is_identification) |
| `IiI` | [2 · Intelligence Is Inevitable](doc:2_intelligence_is_inevitable) |
| `Matter` · `PiR` | [3 · Permanence Is Resistance](doc:3_permanence_is_resistance) |
| `DiD` | [4 · Difference Is Distance](doc:4_difference_is_distance) |
| `InI` | [5 · Interference Is Intelligence](doc:5_interference_is_intelligence) |
| `DiC` | [6 · Distance Is Cost](doc:6_distance_is_cost) |
| `Nav` · `NiG` | [7 · Navigation Is Geometry](doc:7_navigation_is_geometry) |
| `Order` · `OiO` | [8 · Order Is Object](doc:8_order_is_object) |
| `Law` | [9 · Law Is Target](doc:9_law_is_target) |
| `Play` | [10 · Play Is Proof](doc:10_play_is_proof) |
| `Basis` | [B · Basis Is Residue](doc:B_basis_is_residue) |
| `Search` · `GA` | [GA · Gradient Is Alignment](doc:GA_gradient_is_alignment) |
| `Gen` · `GD` | [GD · Proposal Is Order](doc:GD_proposal_is_order) |
| `Liquid` | [L · Learning Is Liquid](doc:L_learning_is_liquid) |
| `Read` | [R · Reading Is Order](doc:R_reading_is_order) |
| `Spec` | [E2 · The Solvability Criterion](doc:E2_solvability_criterion) |
| `Projection` | [AI · The Architecture of Learning Machines](doc:AI_architecture_of_learning_machines) |

> Tip: A section code inside a tag is the paper's own numbering — `§5.22`, `G2c`, `N5`, `O9`, `LT3`, `PP10`, `B4`. In the paper, that code lands you on the exact claim.

## Topics

### The ground — below the floor

- <doc:0_existence_is_finite>

### The numbered series

- <doc:1_verification_is_identification>
- <doc:2_intelligence_is_inevitable>
- <doc:3_permanence_is_resistance>
- <doc:4_difference_is_distance>
- <doc:5_interference_is_intelligence>
- <doc:6_distance_is_cost>
- <doc:7_navigation_is_geometry>
- <doc:8_order_is_object>
- <doc:9_law_is_target>
- <doc:10_play_is_proof>

### The lettered papers

- <doc:B_basis_is_residue>
- <doc:GA_gradient_is_alignment>
- <doc:GD_proposal_is_order>
- <doc:L_learning_is_liquid>
- <doc:R_reading_is_order>
- <doc:E2_solvability_criterion>
- <doc:AI_architecture_of_learning_machines>
